"""PFF Core package namespace.

The initializer stays intentionally inert so importing ``poietics.pff.model``
does not also load registry or validation policy.  Public records and
operations are imported from their owning submodules.
"""
